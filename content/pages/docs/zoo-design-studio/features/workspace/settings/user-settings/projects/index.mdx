---
title: Projects
excerpt: Configure default project creation and project library behavior in Zoo Design Studio.
sidebarPosition: 3
---

The `Projects` section contains user-level defaults used when creating new projects and managing
project library locations.

## Included Settings

### Default Project Name

The default project name to use when creating a new project.

### Project Libraries

Project libraries control which project sources appear on Home.

On desktop, you can add, rename, reorder, and remove Directory libraries. A Directory library points
to a folder on your computer. Removing the library from settings does not delete the projects in that
folder.

When cloud sync is available, the `Personal Cloud` library appears as a Cloud library. Its settings
row shows the resolved local storage path for the cloud working copy.

On web, Project Libraries are limited to Personal Cloud. Browser storage is not a normal local folder,
so Zoo Design Studio treats browser projects as cloud-backed projects rather than showing local-only
browser libraries.

For the full storage model, see
[Project Libraries](/docs/zoo-design-studio/features/data-management/project-libraries) and
[Cloud Sync](/docs/zoo-design-studio/features/data-management/cloud-sync).
